% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/UtilitiesFunc.R
\name{chromatogram_overwrite}
\alias{chromatogram_overwrite}
\title{Static base-R chromatogram renderer with a corrected color palette.}
\usage{
chromatogram_overwrite(
  obj,
  trim5 = 0,
  trim3 = 0,
  showcalls = c("primary", "secondary", "both", "none"),
  width = 100,
  height = 2,
  cex.mtext = 1,
  cex.base = 1,
  ylim = 3,
  filename = NULL,
  showtrim = FALSE,
  showhets = TRUE,
  colors = "default"
)
}
\arguments{
\item{obj}{A sangerseq or SangerRead instance.}

\item{trim5}{Integer; number of bases to mark as 5' trimmed.}

\item{trim3}{Integer; number of bases to mark as 3' trimmed.}

\item{showcalls}{One of \code{"primary"}, \code{"secondary"},
\code{"both"}, or \code{"none"}.}

\item{width}{Bases per row.}

\item{height}{Plot height per row (relative units).}

\item{cex.mtext}{Text size for marginal annotations.}

\item{cex.base}{Text size for base-call labels.}

\item{ylim}{Maximum y-axis multiplier (relative to robust mean).}

\item{filename}{Optional path to write a PDF.}

\item{showtrim}{Logical; if TRUE, shade the trim regions.}

\item{showhets}{Logical; if TRUE, mark heterozygous positions.}

\item{colors}{Either \code{"default"}, \code{"cb_friendly"}, or a
length-5 character vector of hex colours.}
}
\value{
Invisibly returns NULL; called for its side effect of
  plotting (or writing to \code{filename}).
}
\description{
Reimplementation of \code{sangerseqR::chromatogram} with a fix for
base color rendering. Intended for static (PDF / PNG) export. For
interactive embedding in Shiny see \code{\link{chromatogram_plotly}}.
}
\examples{
data(sangerReadFData)
\donttest{
chromatogram_overwrite(sangerReadFData)
}
}
